class <%= goal.cs -%>
  include DataMapper::Resource

  property :id, Serial
<%= goal.fields.map { |k, v | DmModel.property_string(k,v) }.join("\n") %> 

<% goal.associations.each do |k, v| -%>
  <%= DmModel.association_string(k, v)  -%>
<% end -%>
end
